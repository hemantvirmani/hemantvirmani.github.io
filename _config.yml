# Tells Jekyll to look for these files and apply specific settings
defaults:
  - scope:
      path: "last.md"  # Target your specific file
    values:
      layout: "none"    # Set the layout to 'none' to remove all theme components

